# godoc

A client tool that helps you parse struct-based configurations in Golang projects into documentation

## Usage

Install the CLI tool:

```shell
go install github.com/hjhsamuel/godoc
```

How to use it:

```shell
godoc --help
```